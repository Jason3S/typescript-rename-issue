{
  "name": "server",
  "version": "1.0.0",
  "description": "server workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf node_modules dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
